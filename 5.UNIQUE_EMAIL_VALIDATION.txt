CREATE OR REPLACE TRIGGER EMAIL_VALIDATION
BEFORE INSERT ON STUDENT 
FOR EACH ROW
DECLARE
	V_COUNT NUMBER;
BEGIN
	SELECT COUNT(*)INTO V_COUNT FROM STUDENT WHERE 	EMAIL=:NEW.EMAIL;
	
	IF V_COUNT>0 THEN
		RAISE_APPLICATION_ERROR(-20002,'DUPLICATE EMAIL 		ID');
	END IF;
	
	
END;
/