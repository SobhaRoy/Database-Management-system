DECLARE
	V_NAME EMP.ENAME%TYPE;
BEGIN
	SELECT ENAME INTO V_NAME FROM EMP WHERE JOB='SALESMAN';
	DBMS_OUTPUT.PUT_LINE(V_NAME);
EXCEPTION
	WHEN TOO_MANY_ROWS THEN
		DBMS_OUTPUT.PUT_LINE('TOO MANY ROWS');
END;
/