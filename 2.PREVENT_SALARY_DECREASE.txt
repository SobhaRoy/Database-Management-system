CREATE OR REPLACE TRIGGER PREVENT_SALARY_DECREASE BEFORE
UPDATE OF SALARY ON EMPLOYEE 
FOR EACH ROW
BEGIN
	IF :NEW.SALARY<:OLD.SALARY THEN
		RAISE_APPLICATION_ERROR(-20001,'SALARY SHOULD NOT DECREASE');
	END IF;
END;
/